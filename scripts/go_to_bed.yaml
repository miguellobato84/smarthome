go_to_bed:
  alias: go_to_bed
  sequence:
  - action: scene.turn_on
    target:
      entity_id: scene.ambiente_off
  - action: automation.trigger
    target:
      entity_id: automation.persiana_estudio
  mode: single
